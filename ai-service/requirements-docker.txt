# AI 服务 Docker 构建用精简依赖清单
# 仅包含代码中实际 import 的包，剔除 torch/transformers/sentence-transformers 等
# 未直接使用的重依赖，以显著减小镜像体积与构建时间。
# 本地开发仍使用完整版 requirements.txt（pip freeze 冻结清单）。
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3
chromadb==1.5.9
dashscope==1.27.1
langchain==1.3.15
langchain-chroma==1.1.0
langchain-community==0.4.2
langchain-openai==1.6.0
langchain-text-splitters==1.1.2
